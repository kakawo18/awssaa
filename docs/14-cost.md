# 第14章 第4分野：コストを最適化したアーキテクチャの設計（出題比率 20%）

> **この章の使い方**：コスト問題の正解は、ほぼ**「使った分だけ」「安いクラスへ移す」「無駄を止める」「コミットして割引」**の4パターンに収まる。**要件（性能・可用性）を落とす選択肢は不正解**であることに注意。

---

## 14.1 コスト管理

| 要件 | 答え |
|---|---|
| 何にいくらかかっているか分析、将来予測、RI/SP推奨 | **Cost Explorer** |
| 予算超過を**事前に**アラート、超過時に自動アクション | **AWS Budgets** |
| 時間単位・リソース単位の**最も詳細な**データを自分で分析 | **CUR**（S3 → Athena/QuickSight） |
| 部門・プロジェクト別に按分したい | **コスト配分タグ**（＋タグポリシーで統制） |
| 複数アカウントの請求をまとめ、割引を共有 | **Organizations 一括請求** |
| 無駄なリソース（未使用EIP、アイドルRDS、低使用率EC2）の発見 | **Trusted Advisor** |
| インスタンスの適正サイズの推奨 | **Compute Optimizer** |
| 異常な支出の自動検知 | AWS Cost Anomaly Detection |

---

## 14.2 ストレージとコスト最適化

| 状況 | 打ち手 |
|---|---|
| アクセス頻度が下がっていくデータ | **ライフサイクルポリシー**でIA→Glacier→Deep Archiveへ自動移行 |
| アクセスパターンが読めない | **S3 Intelligent-Tiering**（監視料金のみ、取り出し料金なし） |
| 再生成可能なデータ | One Zone-IA |
| 完了しなかったマルチパートアップロードの残骸 | ライフサイクルルールで中止・削除 |
| 古いバージョンやスナップショットが溜まる | 非現行バージョンの失効、**DLM/AWS Backup**の保持期間設定 |
| EBSが過剰にプロビジョニングされている | gp2→**gp3**、サイズ/IOPSの見直し、未アタッチボリュームの削除 |
| EFSのコスト | ライフサイクル管理でIAへ、One Zone |
| データ転送料が高い | CloudFrontでオフロード、**VPC内からのS3/DynamoDBはゲートウェイエンドポイント（追加料金なし、NAT処理料も不要）**、同一AZ内通信、**AZ間・リージョン間転送を減らす** |
| ログの保管費用 | CloudWatch Logsの保持期間設定、S3へエクスポートしてGlacierへ |

**覚え方**：**AWSへの受信（イン）は原則無料、送信（アウト）とAZ/リージョン間が有料**。

---

## 14.3 コンピューティングとコスト最適化

| 状況 | 打ち手 |
|---|---|
| **中断を許容できる**バッチ、CI、ビッグデータ | **スポットインスタンス**（最大90%引き、EMRのタスクノード、Fargate Spot、Batch）。「バースト分だから」ではなく、**中断されても再実行できるか**と**必要な容量が確保できるか**を確認して選ぶ |
| 常時稼働が1年以上確実 | **Savings Plans**（柔軟）／**RI**（特定条件でさらに安い） |
| 開発環境が夜間・休日も動いている | **スケジュールされたスケーリング／Instance Schedulerで停止** |
| 使用率が低いインスタンスが多い | 適正サイズ変更（Compute Optimizer）、**Gravitonへ移行** |
| リクエストが断続的・少量 | サービスごとに**課金が止まる条件**が違う：**Lambda**＝呼び出しがなければ課金なし／**Fargate**＝**タスクが起動中なら**リクエストがなくても vCPU・メモリ分の課金（0台にスケールインして初めて止まる）／**Aurora Serverless v2**＝最小 ACU まで下がるが、自動一時停止は対応バージョン・設定に依存し、それ以外は最小容量分の課金が続く |
| ライセンス費用が重い | Dedicated HostでBYOL、Linuxへの移行、Babelfish |
| 冗長化しすぎ | 可用性要件に応じたAZ数・台数の見直し（**要件を下回らない範囲で**） |

**組み合わせの定番**：ベース負荷はRI/Savings Plans、変動分はオンデマンド、**中断を許容できる**処理はスポット。「バースト分＝スポット」と機械的に当てはめず、中断されたときにユーザー影響がないかを見る。

---

## 14.4 データベースとコスト最適化

| 状況 | 打ち手 |
|---|---|
| DBが常時稼働・長期利用 | **リザーブドインスタンス（RDS/ElastiCache/Redshift/OpenSearch）** |
| 開発・検証DBが夜間も起動 | RDSの停止（最大7日で自動起動する点に注意）、Aurora Serverless v2 |
| 読み取り負荷にレプリカを増やし続けている | **ElastiCacheで肩代わり**（レプリカより安くなることが多い） |
| DynamoDBの負荷が安定している | プロビジョンドキャパシティ＋Auto Scaling（オンデマンドより安い） |
| DynamoDBに古いデータが溜まる | **TTL**で自動削除、S3へアーカイブ |
| 大きなオブジェクトをDBに入れている | **S3に本体、DBにはポインタ** |
| 分析のためだけに大きなDBを維持 | S3＋Athena、Redshift Serverless、Spectrum |
| スナップショットが増え続ける | 保持ポリシーの設定、不要な手動スナップショットの削除 |

---

## 14.5 その他のコスト削減策

- **サーバーレス・マネージドへの移行**：運用人件費まで含めた「総コスト」が下がる、という選択肢は正解になりやすい。
- **アーキテクチャでの削減**：API Gatewayから**AWSサービスを直接統合**してLambdaを省く、Step FunctionsからSDK直接呼び出し、CloudFrontのキャッシュヒット率向上、SQSの**ロングポーリング**でAPIコール削減。
- **リージョンの選択**：料金はリージョンで異なる。レイテンシ・法規制の要件がなければ安いリージョンを選ぶ。
- **Snowball vs ネットワーク転送**：大容量かつ回線が細い場合は物理移送の方が安く速い。
- **削除し忘れの回収**：未使用のEIP（アタッチされていないと課金）、古いスナップショット、停止中インスタンスに紐づくEBS、アイドルのNAT Gateway/ELB。
- **タグ付けの徹底**：可視化できないコストは削減できない。タグポリシー＋コスト配分タグ。

**「安い」の3つの意味を区別する**

| 設問の言い方 | 何を最小にするか | 選ばれやすいもの |
|---|---|---|
| **運用負荷を最小に** | 人手・運用工数 | マネージド／サーバーレス（AWS利用料は上がることもある） |
| **AWS利用料を最小に** | 請求額 | RI/SP、スポット、ライフサイクル、適正サイズ |
| **総コスト（TCO）を最小に** | 利用料＋人件費＋機会損失 | 「サーバーレス移行で運用人件費まで含めて下がる」型 |

「サーバーレス」という分類だけでは費用は決まらない。Fargate はホスト管理を不要にするが、リクエストがなくてもタスクが稼働中なら料金が発生する。**稼働時間、割り当て資源、周辺サービス（NAT、ELB、ログ）を含めて比較**する。

**試験での注意**：「最もコスト効率が高いのはどれか」の設問でも、**要件（可用性・RTO・レイテンシ）を満たさない選択肢は不正解**。まず要件を満たす候補に絞ってから、その中で最も安いものを選ぶ。

---

## 14.6 設計ケース：昼夜で負荷が変動するアプリの費用比較

**要件**：社内向けWebアプリ。日中（9〜18時）はリクエストが多く、夜間はほぼゼロ。可用性は営業時間中に必要（単一AZ障害で止まらない）。運用担当は1人。

**絶対条件**：営業時間中は AZ 障害で止まらない。夜間に落ちても翌朝までに復旧すればよい。

**候補**：(A) EC2 ASG（複数AZ）＋Savings Plans、(B) ECS on Fargate（複数AZ）＋スケジュールスケーリング、(C) Lambda＋API Gateway、(D) 単一 EC2 を夜間停止。

**課金単位で比較する**

| 候補 | 課金の単位 | 夜間の課金 | 運用工数 |
|---|---|---|---|
| A: EC2 ASG＋SP | インスタンス時間。SP は**コミット額が常時課金**され、その範囲内の利用はコミットで賄われる（二重には課金されない） | スケールインしても **SP のコミット分**は残る（min 台数がコミット内なら、その分は追加請求なし。コミットを超える日中の増分だけオンデマンド） | OS パッチ、AMI 管理 |
| B: Fargate | タスクの vCPU・メモリ×秒 | **タスクを 0〜最小にスケールインした分だけ減る**。起動中なら課金 | ホスト管理なし、イメージ管理は要る |
| C: Lambda | リクエスト数×実行時間×メモリ | **呼び出しがなければ 0** | コードの分割、15分制限、コールドスタート |
| D: 単一 EC2 夜間停止 | インスタンス時間（停止中は EBS のみ） | 最小 | 最小だが**可用性条件を満たさない** |

**条件を満たさない候補と理由**：D は営業時間中の AZ 障害で止まるので絶対条件に反する。「最安」でも不正解。

**採用構成**：アプリが既にコンテナ化されているなら B（複数AZの Fargate、日中は min 2、夜間はスケジュールで min 1 か 0＋ALB）。ステートレスな短時間処理に分解できるなら C が夜間ゼロで最も安い。既存の EC2 前提で改修できないなら A（ベース分だけ SP、日中の増分はオンデマンド）。

**残る制約**：B で夜間 0 台にすると翌朝の初回起動に時間がかかる（スケジュールで前もって起動）。ALB・NAT Gateway は時間課金なので、アプリを止めても残る。C は夜間ゼロだが、日中の高頻度リクエストでは EC2/Fargate より高くなることがある。

**条件を1つ変えると**

| 変えた条件 | 変わる判断 | 理由 |
|---|---|---|
| 24時間一定の高負荷 | A（RI/SP でベースを固定） | 常時稼働ならコミット割引が最も効き、サーバーレスの従量課金は割高 |
| 可用性要件なし（開発環境） | D（夜間停止）または Instance Scheduler | 要件がなければ単一 AZ・停止が最安で正解になり得る |
| 「運用負荷を最小に」が主題 | B か C | 利用料ではなく運用工数を最小にする設問。EC2 のパッチ運用は工数 |

**理解確認**：「Fargate はサーバーレスなので夜間の料金はゼロになる」は正しいか。→ **誤り**。Fargate はタスクが起動している間は課金される。ゼロにするには**タスク数を 0 にスケールイン**する設定が要り、ALB 等の周辺は残る。呼び出しがないときに課金がないのは Lambda。

**公式出典**
- [AWS Fargate 料金](https://aws.amazon.com/fargate/pricing/)
- [Well-Architected 信頼性の柱：DR 戦略](https://docs.aws.amazon.com/wellarchitected/latest/reliability-pillar/rel_planning_for_recovery_disaster_recovery.html)

---

[目次に戻る](./README.md) ／ 付録：[横断比較表](./90-comparison.md) ／ [キーワード逆引き](./91-keywords.md)
